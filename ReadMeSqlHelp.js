// *  to open shell
// python manage.py shell

// * import statements
// from blog.models import Post
// from django.contrib.auth.models import User

// * to get the queryset (all users)
// User.objects.all()

// * to get the queryset (1st user same for last user)
// User.objects.first()

// * to filter user
// User.objects.filter(username = 'Shriyash')


// user =  User.objects.filter(username = 'Shriyash')

// user.id 
// user.name
// ?-------------------------------

// Post.objects.all()
// * to create a post (type all in one line )
// post1 = Post(
//     title = 'Blog1',
//     content = 'first one',
//     author = user 
// )


